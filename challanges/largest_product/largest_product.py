def LargestProduct(array):
    # for iterates over outer array
    # for loop iterates over inner array
        # multiply the 2 numbers
        # compare to see which product is biggest
    return running_largest_product