


def BinarySearch(sorted_array, search_key):
    # find middle index
        # middle_index = sorted_array.length / 2

    # if search key == sorted_array[middle_index]
        # done! return middle_index

    # elif search key > sorted_array[middle_index]
        # search_key must be in top half of list
        # keep searching and splitting until match is found

    # else search key must be in bottom half of list
        # keep searching and splitting until match is found

    # return -1 if no match found

